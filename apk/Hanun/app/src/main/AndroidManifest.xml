<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dipdip.android.dip.com.hanun">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/iconsepeda"
        android:label="THE SPEDA"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DaftarActivity"></activity>
        <activity android:name=".HomeActivity"></activity>
        <activity android:name=".RiwayatActivity"></activity>
        <activity android:name=".AkunActivity"></activity>
        <activity android:name=".GrafikActivity"  android:screenOrientation="landscape"></activity>
        <activity android:name=".HasilActivity"></activity>
        <activity android:name=".NotifikasiActivity"></activity>
        <activity android:name=".Notifikasi2Activity"></activity>
    </application>

</manifest>